/**
 * 목적:
 * 유저모니터링 데이터를 최대한 새로운 리포트 등록 없이 스스로 찾을 수 있게끔 한다.
 * 차세대 분석센터에서 사용할 것 같은 방식으로 구현한다. ex. web-worker
 *
 * # conditions
 * x축, y축에 변수를 넣을 수 있다.
 *
 * ## x축
 * x축엔 하나의 변수만 들어갈 수 있다.
 * ?? x축엔 숫자 타입은 들어올 수 없다.
 *
 * ## y축
 * y축은 숫자 타입만 들어갈 수 있다.
 *
 *
 */
function SelfServiceReport() {
	return <div></div>
}

export default SelfServiceReport
